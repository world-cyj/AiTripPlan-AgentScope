server:
  port: 8085

spring:
  main:
    lazy-initialization: true

agentscope:
  a2a:
    server:
      enabled: ${A2A_SERVER_ENABLED:false}
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
